pub struct DeltaWriter;

impl DeltaWriter {
    #[allow(clippy::too_many_arguments)]
    pub fn new(
        _bucket: &str,
        _prefix: &str,
        _endpoint: Option<&str>,
        _region: &str,
        _access_key: &str,
        _secret_key: &str,
    ) -> Self {
        todo!()
    }
}
